{
  "name": "embedding-service",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "node server.js",
    "setup:python": "bash ./setup-python-env.sh",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "../backend/node_modules/.bin/eslint .",
    "lint:fix": "../backend/node_modules/.bin/eslint . --fix"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "**/*.js",
      "!node_modules/**",
      "!coverage/**"
    ]
  }
}
